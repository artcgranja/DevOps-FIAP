POSTGRES_USER=esg
POSTGRES_PASSWORD=changeme
POSTGRES_DB=cidades_esg

ASPNETCORE_ENVIRONMENT=Production
ConnectionStrings__Default=Host=db;Port=5432;Database=cidades_esg;Username=esg;Password=changeme

# GHCR owner (your GitHub username or org) — used by staging/prod compose files
GHCR_OWNER=arthurgranja

# Image tag override (local | staging | prod | sha-xxxxxxx)
IMAGE_TAG=local
